package testCases;

import userActions.*;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;

public class ScrollPipeline_TC {

	
	
	/*Actions builder = new Actions(AppSession);
		
	 new Actions(AppSession).sendKeys(Keys.ARROW_RIGHT).perform();
	
	
	builder.keyDown(Keys.LEFT_CONTROL).sendKeys(Keys.ARROW_DOWN).keyUp(Keys.LEFT_CONTROL).build().perform();
	String receiveValveDist = AppSession.findElementByXPath("//*[contains(@Name, 'Dist.')]").getText();
	int endDistance=Integer.parseInt(receiveValveDist.substring(7,receiveValveDist.lastIndexOf(".")));   
	builder.keyDown(Keys.LEFT_CONTROL).sendKeys(Keys.ARROW_UP).keyUp(Keys.LEFT_CONTROL).build().perform();
	
	WebElement myDist =  AppSession.findElementByXPath("//*[contains(@Name, 'Dist.')]");
	String blah;
	int currentDisance =0;
	
	if (currentDisance < endDistance) {
		builder.keyDown(Keys.ARROW_RIGHT).build().perform();
		while (currentDisance < endDistance) {
			blah = myDist.getText();
			blah = blah.substring(7, blah.lastIndexOf("."));
			currentDisance = Integer.parseInt(blah);
		}
		builder.keyUp(Keys.ARROW_RIGHT).build().perform();
	}
	



	
	
	//System.out.println(blah.lastIndexOf(" ft"));
	//System.out.println(blah.substring(7,blah.lastIndexOf(" ft")));
	System.out.println("test");
*/	
}
